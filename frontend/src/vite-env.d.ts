/// <reference types="vite/client" />

// Injected at build time by vite.config.ts (#26): the git tag when this commit
// is tagged, otherwise the short commit hash.
declare const __APP_VERSION__: string;
